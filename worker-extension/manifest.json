{
  "manifest_version": 3,
  "name": "Flow2API Worker",
  "version": "3.3.1",
  "description": "Flow2API worker for staff: install once, stay signed in to Google Labs, done. Supplies real-browser reCAPTCHA tokens, keeps the login fresh, and mints+redeems from the same residential IP automatically. No setup.",
  "permissions": [
    "storage",
    "tabs",
    "scripting",
    "cookies",
    "alarms",
    "notifications",
    "proxy",
    "webRequest",
    "webRequestAuthProvider"
  ],
  "host_permissions": [
    "<all_urls>"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "options_page": "options.html",
  "action": {
    "default_title": "Flow2API Worker",
    "default_popup": "options.html"
  }
}