{
  "manifest_version": 3,
  "name": "Flow2API Worker",
  "version": "2.8.0",
  "description": "All-in-one Flow2API worker: supplies real-browser reCAPTCHA tokens (persistent tab), keeps the Google Labs login fresh, and reports its residential egress IP + browser UA so the backend redeems from the same IP (reCAPTCHA consistency).",
  "permissions": [
    "storage",
    "tabs",
    "scripting",
    "cookies",
    "alarms",
    "notifications",
    "proxy",
    "webRequest",
    "webRequestAuthProvider"
  ],
  "host_permissions": [
    "<all_urls>"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "options_page": "options.html",
  "action": {
    "default_title": "Flow2API Worker",
    "default_popup": "options.html"
  }
}