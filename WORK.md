# Shared Agent Status

Last updated: 2026-09-23 (cookie sync / away mode)

## Active

- 2026-09-26 (owner: nishatmini16 logged out again → "debug harder"; option 2 = fix the box; owner gave ports 8011–8020): **flow-ultra-01 rebuilt — branded Google Chrome 154 (was Chromium), its OWN proxy port 8016 (Charter, Ashburn; exit 189.104.87.117), every byte through it from the first request.** Evidence that drove it: Google revoked the account ~13 h after the 24 Sep login and ~40 s after a fresh login on 26 Sep with no server action in between; old port 8005 was shared by 6 active Google accounts (every pool port had 5–11); Codex found Chrome (and Chrome sync) went DIRECT from the Hetzner datacenter IP until the extension applied its proxy at every start. Now: `proxy-forward.py` (container loopback 127.0.0.1:3128 → site.json upstream with credentials) + `--proxy-server`, WebRTC limited to proxied UDP; branded Chrome ignores `--load-extension` and the Load unpacked dialog cannot open in the container, and a CDP-loaded extension lasts one session, so `register-ext.py` registers `/opt/ext` via CDP `Extensions.loadUnpacked` at EVERY start (debug port on container loopback only; same path ⇒ same id `ghppbo…`); success requires a returned id, 5 tries, else the container stops; `/tmp/fu-ready` gates the updater's verify. Updater fixes: compare the version part of `-<timestamp>` release folders, never reuse a folder (`mv -T`; it had nested a copy and kept the old port). Entrypoint prune limited to `/opt/releases`/`/opt/ext` (it had deleted Chrome's built-in PDF viewer). Chrome is signed in at PROFILE level ("Continue as Tuba"), Labs signed in; server auto-re-enabled token 93 at 07:21Z; still signed in after a restart. Google asked a phone confirmation on the new device (owner tapped). **Flow pool: ports 8011–8015, 8017–8020 added (19 ports); 8016 kept OUT — private to flow-ultra-01, never add it to the pool.** Posted with the existing connection token (POST /api/plugin/config regenerates a random token if omitted — would disconnect every staff browser). Fleet at 07:25Z: 22 of 72 accounts active; last hour 789 ok / 822 failed, 599 of them "no token available" (pool exhausted by the sign-out wave since 23 Sep). Codex round `tmp/codex_codex_flow_ultra_chrome_prompt_0926-1254.md` (ship-with-changes; both blockers fixed: direct egress before the extension loads, silent registration failure). Old Chromium profile kept at `/srv/flow-ultra/profile` for rollback.

- 2026-09-24 (owner, from an ai-reels session: "reels must use Ultra, no watermark"): **new client policy `ai-reels` = image `ultra` / video `ultra`**, added LIVE through the admin API (`POST /api/client-policies`, hot-reloaded, read back). No code change here. ai-reels' worker now hard-codes `X-Flow-Client: ai-reels` (ai-reels `5525609`, `c4b0fd0`). Proof: a tagged request ran on token 55 (`PAYGATE_TIER_TWO`, not the pinterest-reserved 93), watermark `not-detected`. Callers seen in 3 days before this: `default` 17,642, `pinterest-factory` 10,744, `cookie-test` 7. Shares the 5 unreserved Ultra accounts with pinterest-factory; when none is free Flow answers 503 `client_policy_no_account`, which ai-reels now treats as busy (retry), not a failure.

- 2026-09-24 (owner: "find a way to auto update the extension" for the cloud Ultra browser): **LIVE — worker extension 3.7.3 + `docker/flow-ultra/` (PR #27 + follow-ups on main)**. The cloud browser (`flow-ultra-01`, cf-worker-01) now follows every version published in the admin: `flow-ultra-ext-sync.timer` (10 min) → `ext-sync.sh` downloads, validates, installs `/srv/flow-ultra/releases/<v>`, graceful `docker restart -t 30`, verifies the container runs `<v>` before marking done (kept the previous release; a failed activation retries). Proven end to end: 3.7.1 → 3.7.2 → 3.7.3 delivered by the updater alone. Box-only overrides live in `/etc/flow-ultra/site.json` → generated `site.js` in each release, loaded with `importScripts` (a service-worker `fetch()` of the packaged file did NOT work on Chromium 153); the upload endpoint refuses a zip containing `site.json`/`site.js`. Extension fixes shipped on the way: cookie-triggered pushes rate-limited with a persisted stamp (10 min while the grant is dead; the loop had produced 216 server lines in 10 min), server `pong` + client dead-socket detection (50 s, a proxy kept a dead socket "open" for 25 min after a deploy — now the box re-registers ~10 s after every redeploy). Container lessons (all in entrypoint.sh): terminate Chromium first and wait (profile flush); remove the stale X lock (`docker restart` keeps /tmp → Xvfb refused, restart loop); clear the profile's `Service Worker` cache at start (Chromium fired "installed" but kept running the OLD cached worker script — twice); prune extension entries from any path other than `/opt/ext` (a symlink experiment left a second worker identity running); copy the release into a real `/opt/ext` (a symlink changes the extension id and its route key). Codex round `tmp/codex_codex_server_autoupdate_prompt_0924-1654.md` (ship-with-changes, 3 blocking points folded in). **Token 93 (`nishatmini16@gmail.com`) is back: owner signed in over noVNC at 12:45Z (a complete login — `.google.com` cookies present; the 12:24 attempt had only `.google.co.in` ones and read as "signed out"), the worker (3.7.3, cookie sync ON) pushed, the server verified the credential and auto-re-enabled the token at 12:46:05Z; a pinterest-tagged test image at 12:49Z was served by token 93, success, clean corner.** noVNC path for the next time: `flow-ultra-novnc` unit on the box, tunnel from the Mac, http://localhost:6081/vnc.html.

- 2026-09-23 (owner: build the cookie-sync idea "so it keeps generating after staff closes the laptop"): **cookie sync / away mode** — worker extension **3.7.0** shares the Chrome profile's Google login (the cookies Chrome sends to accounts.google.com, JSON list) in every session push; the server stores it per account (`google_cookies`, `protocol_mode=protocol`, `login_account`, new `google_cookies_updated_at` + `google_cookies_seq` columns, guarded ALTER verified on the live schema and a fresh DB) and, when the Labs session dies, replays Labs' "Sign in with Google" itself (`protocol_login.py`) THROUGH the account's proxy (`proxy_url` else `redeem_proxy_url`; no proxy = no login, never from the datacenter IP). Plan `tmp/cookie_sync_plan.md`, Codex `tmp/codex_cookie_sync_plan_0923-2154.md` (ship-with-changes, 8 blocking points, all folded in): dedicated `POST /api/plugin/cookie-sync {action:clear}` so OFF deletes the server copy without any Google round-trip (retried every minute until acknowledged, popup shows "Deletion pending"); client `cookie_sync_seq` so a slow ON push can never undo a later OFF; identity guard in `_do_refresh_at` (a session for another email is `account_mismatch`, never promoted); the push endpoint derives a session from the cookies when the pushed one is dead at the FIRST `st_to_at`, on `st_expired`, and on `at_stale`; cookies stored whenever the push proved the email (also `at_stale`); `protocol_login` attaches the Google jar only to google.com hosts and matches the Labs callback exactly (a foreign redirect ends the login); structured `reason` (network/timeout → 503 = retry, rejected → 400); healer loop now logs in ONLY for `auto_st_expired`/`auto_at_stale` rows (active rows heal on demand in `_handle_at_stale`/`_try_refresh_st`), re-reads the row after the login and re-enables both reasons on a verified credential; `GET /api/tokens` no longer returns `google_cookies`/`login_password` values (flag + timestamp only; admin table shows "away ✓ N min ago"). Extension: `cookie_sync.js` (switch "Keep working when I'm away", default ON, status line from what the last push proved; re-push on SID/HSID/SSID/LSID/*PSID/*PSIDTS changes, 30 s debounce, ≥10 min gap; a push with no Labs cookie but a Google login goes out cookies-only so the server can sign the account in). Docs `docs/cookie-sync.md`. Tests: `tests/test_cookie_sync.py` (23), `tests/ext/cookie_sync.test.mjs` (6); suite 393 pass / 1 pre-existing. Pushback on Codex recorded in the plan: st_to_at/verification stay direct (36 accounts refresh their AT from the server IP every day without issue). **Release gate PASSED 2026-09-23 16:46-16:53Z** (backend 417c561+3aef3d3 deployed first; throwaway Chrome = copy of the owner's Profile 6 = account 54, loading the repo's `worker-extension` folder): push carried 24 cookies (`SID,HSID,SSID,APISID,SAPISID` present); 54's browser session had a stale grant since 09-22 → the server derived a fresh Labs session from the cookies through 54's Oxylabs :8003 in 1.5 s, verified it and re-enabled the account (`derived: true`); `GET /api/tokens` shows `google_cookies_set` + timestamp only; Chrome closed + route key set to a bogus value (worker offline) + `st` overwritten with garbage → `POST /api/tokens/54/refresh-at` → `[AT_REFRESH] ST expired → [ST_REFRESH] protocol → succeeded → AT verified (credits 22323)`; then a pinned image (200, 22 s) and a 4 s Omni video (200, 41 s) with `[FALLBACK_MINT] source=server`; switch OFF → `POST /api/plugin/cookie-sync clear` → row `protocol_mode=session`, cookies empty; ON → stored again. Bug found by the gate and fixed (3aef3d3): a dead ST made Labs answer `{}` → `KeyError access_token` → "unknown" → NO repair ever ran (pre-existing; explains part of the ~15/h `AUTH/ST_EXPIRED`). Codex code review `tmp/codex_cookie_sync_review_0923-2217.md` (ship-with-changes, hold rollout; 7 blocking) → all folded in: atomic sequenced cookie write (`db.update_token_cookie_sync`, positive seq required, equal seq idempotent), extension generation counter so an old reply can never flip the switch state (`stale` ≠ cleared; pending clear re-sent with a fresh seq), stored-credential-first guard + per-token recovery lock + 23 s request budget before any Google login replay (the gate's 3 replays in 10 s were this), transport failures at the first `st_to_at` → 503 (never a replay), cookies kept on `at_stale` even when the replay fails, cookies-only push after a Labs sign-in bounce in persistent mode (login breaker untouched by such a push), healer re-reads the row right before re-enabling, HTTPS-only/default-port Google hops + Labs-origin-only callback follow-ups. Tests 28 Python + 7 Node; suite 398 pass / 1 pre-existing. Pushback kept: `st_to_at`/credits verification stays direct from the server IP (36 accounts do it daily); clear-by-token-id keeps the shared connection-token trust level (documented). Reviewed build (67c4c41) re-verified live 17:03-17:12Z: throwaway Profile 6 copy → OFF deleted / ON stored; second throwaway (copy of Profile 13 = account **55**, ext 3.6.4→3.7.0) → 24 cookies stored → browser closed + bogus route key + dead `st` → `refresh-at` healed through :8004 in 1.6 s (credits 24575) → pinned **image 200 (31 s) with `[FALLBACK_MINT] source=server`**. **Open finding (server-fallback, not cookie-sync):** every server-minted `VIDEO_GENERATION` token was refused by Google (`PUBLIC_ERROR_UNUSUAL_ACTIVITY`, 3/3 on 55, and 54's image mints were refused ~50%: strike 4 → 10 min pause), while staff-account image mints pass (20 ok today). So with the laptop closed, IMAGES work end-to-end; VIDEOS still need a real worker browser until the fallback's video score is solved (next task: mint the video action on a project page / real-browser score, see `flow_page_captcha.py`). **3.7.0 PUBLISHED 17:13Z** (`POST /api/ext/upload`; staff see the banner). The owner's own Chrome profiles load the extension from the repo folder, so they run 3.7.0 after their next reload. Both accounts restored (route keys, reservation cleared); throwaway profile copies deleted.
- 2026-09-23 (owner: "then lets build ours fully and properly" — Flow Characters): named person/thing from 1–3 photos, `@Name` in the prompt, kept consistent across images and videos. API: `characters:[{name, images:[data/http URLs]}]` on both OpenAI and Gemini entry points (docs/flow-characters.md). Flow's API was mapped by probing aisandbox-pa with Google's own field-name errors (no browser needed): create `POST v1/flow/entities` (`entity.entityInfo.entityType="CHARACTER"`), photos via `uploadImage` + `flow:copyProjectMedia` into `characterSlot.imageReferenceIndex`, read `GET v1/flow/entities:batchGet?entityIds=`, use `requests[i].referenceEntities:[{entityId}]` + `structuredPrompt.parts[].reference.entity{entityId,handle}` (video: `textInput.structuredPrompt`). No delete route exists; entities are project-scoped, so `flow_characters` caches account+project+name+photo-digest → entityId (row written only after read-back shows the photos; per-key lock; explicit not-found → recreate; other errors fail closed). Guarantees: validation → 400 before any upload; setup failure → 400/502 with NO generation submitted and no account strike. Videos: only ingredients models (r2v / omni, forced onto the reference route), max 3; images max 10. Plan `tmp/characters_plan.md` + Codex `tmp/codex_characters_plan_0923-2014.md` (ship-with-changes; all six points folded in: character-only video routing, Gemini + streaming plumbing, explicit setup errors, race-safe cache, deterministic @Name matching, visual proof as release gate). Old dead `create_flow_entity` (labs tRPC) left untouched. Tests: `tests/test_characters.py` (14) + `tests/test_characters_handler.py` (10). Side effect of probing: five empty projects were created on account 59 by `v1/projects/{id}:xxx` calls (Google treats any verb there as createProject) and deleted again with `DELETE v1/projects/{id}`; one test character "Test Zed" remains in account 59's project (harmless). **Release gate PASSED 2026-09-23 15:06Z (LIVE `14f5ed5`)**: a red toy robot in a yellow hat generated with the plain API was sent back as character `Robo` (1 photo); `@Robo … sunny beach at sunset` (Nano Banana 2 Lite) returned the same robot on a beach, `@Robo waves … along a sunny beach` (Omni Flash 4 s) returned the same robot waving then walking — both inspected frame by frame. First request per account+project pays the setup (~12 s: create + upload + attach + read-back); repeats reuse the entity (`[CHARACTER] reuse`, 11–13 s total). Found and fixed on the way: projects rotate per request and entities are project-scoped, so the second request created a new character each time; a request that carries characters is now steered to the account's project that already holds them (`db.find_project_with_characters`). Not verified: Veo r2v with characters (only Omni + images were run live); a character-only video (no ordinary reference image) worked on Omni. One ad-hoc mint during probing was refused by reCAPTCHA (my quick script, not the production path) — irrelevant to the feature. Leftovers: entities named `Robo` in accounts 55 (4, three now unused), 89 (1) and `Test Zed` in 59; Flow has no delete route.
- 2026-09-23 (owner: "will it work as fallback? plan it, test it, deploy it"): **server-side reCAPTCHA fallback** (`src/services/flow_page_captcha.py`), so the next Google captcha change is one deploy, not 40 staff reloads. Plan `tmp/server_captcha_fallback_plan.md` (rev 3), Codex rounds `tmp/codex_server_captcha_fallback_plan_0923-1718.md` + `…_1723.md` (both ship-with-changes, all blocking points folded in). Proven live before coding: a token minted on `flow.google.com/about` by the container's Playwright Chromium (headed on Xvfb, `--disable-http2 --disable-quic --disable-blink-features=AutomationControlled`, `navigator.webdriver` hidden, NO user-agent override, ~4 s dwell + mouse moves) through account 54's own Oxylabs port and redeemed with THAT Chromium's UA → HTTP 200 image; the same with the account's Mac UA forced → 403 (headed and headless). Chromium hangs on every Google host through these proxies unless `--disable-http2` (curl h2 is fine; cause unknown). What it does: when the worker extension is offline, could not mint, or Google refused its token (transport-classified `flow_fail_class == "RECAPTCHA"` → the rest of that request mints on the server, incl. Omni approval), the server mints through the account's `redeem_proxy_url` (never from the datacenter IP) and redeems with the server Chromium's UA; one Chromium per proxy, cap `server_fallback_max_browsers` (default 3, 1..6), LRU eviction, idle close 10 min, absolute 30 s deadline per mint incl. queue wait. Load balancer: an offline worker no longer excludes an account when flag ON + proxy + this image has Chromium (`is_available()` cached probe). Config `captcha_config.server_fallback_enabled` (default ON) / `server_fallback_max_browsers` (guarded ALTER + fresh CREATE + INSERT/UPDATE + reload; migration verified on the live schema and a fresh DB), admin GET/POST, Settings → Captcha → "Server captcha fallback" card, `/api/captcha/runtime-status?method=extension` reports the pool. Omni approval mint failure now uses the standard "Failed to obtain reCAPTCHA token" text (mint pause, not a strike). Tests: `tests/test_server_captcha_fallback.py` (15), suite 343 pass / 1 pre-existing. NOT promised: headless acceptance (standard `Dockerfile` has no Chromium → fallback reports unavailable, app healthy). **LIVE 2026-09-23 12:07Z** (`3fde038`, then `08942fd` readiness wait + proxy-list validator fix, then the Codex code-review round `tmp/codex_fallback_code_review_0923-1737.md` — ship-with-changes, all 7 items folded in: cancellation-safe, shielded and bounded browser close; the absolute deadline now covers pool-lock wait and eviction; the rejected-token override is committed only when the fallback can actually serve the account; classifier order auth → quota → policy → reCAPTCHA and the urllib path is tagged too; a 60 s idle/over-cap/disabled sweeper task in lifespan; one Playwright driver (lock); the capability probe resolves the executable launch() uses). Live evidence within a minute of deploy, on real staff traffic: accounts 88, 53, 30 (old extensions) → `[FALLBACK_MINT] … outcome=ok` → `RESULT=success`; after 10 min: 32 generations success / 0 fail, pool 17 ok / 0 fail / 6 timeouts (all token 53 on port 8008, slow page load); healthy 3.6.4 accounts (55, 65, 67) never touched it. Cap raised 3 → 5 via the admin API (10 proxy ports were evicting each other; cold launch ~20 s, CPU-heavy on this 8-core box at load ~8). Container RSS 2.9 GB with 3 browsers. Pre-existing bug fixed on the way: the captcha settings page could not be saved at all because the validator rejected the comma-separated proxy list already stored. **CPU lesson (12:24Z):** with 4–5 warm browsers the box hit load 62 on 8 cores and 41% of fallback mints timed out — Flow's animated /about page under Xvfb software rendering costs ~40% of a core per idle browser (the box also runs an OCR container at ~300%). Fix `cbb0d59`: `--disable-gpu`, reduced-motion emulation, images/media/fonts blocked, idle pages parked on about:blank after 60 s (next mint reloads in ~5 s), cap set to **2** via the admin API. After: container CPU 7% (was ~300%), RSS 1.0 GB (was 3.9), fallback 19 ok / 0 timeouts, 31/31 generations success, 12:20Z bucket 91 ok / 8 fail. Watch `[FALLBACK_MINT] outcome=timeout` and `docker stats` before raising the cap again.
- 2026-09-23 **OUTAGE 02:43-10:40Z, zero successful generations from 03:00**, fixed by worker extension **3.6.1** (needs every staff browser to reload the extension). Cause (CONFIRMED, not our code): Google shipped a new Flow frontend build ~02:40Z (`AiSandboxAngularFrontend` `nbnuGi0qoeI` → `xblUEBXqKbA`) with two changes: (1) the backend now accepts only reCAPTCHA tokens minted on **flow.google.com** — a token minted on labs.google/fx (where 3.3.x-3.5.x minted) gets `PUBLIC_ERROR_UNUSUAL_ACTIVITY: reCAPTCHA evaluation failed`; (2) flag `enable_recaptcha_execute_closure_wrap` (`45846838`, default on): the page keeps the genuine `grecaptcha.enterprise.execute` in a closure and overwrites the public one with a wrapper that forces `action:"extension_hijack_detected"`, so any extension calling the public function mints a poisoned token. Proof (same account 54, same minute): labs.google token → 403; flow.google.com token → 200 real image. Rejections escalated the per-account progressive cooldowns until every account was benched ("No token available" 1,369/h). Staff extension versions at the time: 32× 3.3.10 (published 2026-09-04), 3× 3.3.5, 2× 3.3.9 — yesterday's 3.5.2 had reached only the owner + flow-ultra-01, so it was not the cause. Fix: mint in a separate, unowned tab on `flow.google.com/about` (page loads no reCAPTCHA; we load `enterprise.js?trustedtypes=true&render=…` with a Trusted Types policy + the page's script nonce — without `trustedtypes=true` the loader loads but never initialises under Flow's CSP, that was 3.6.0's 25 s hang) and `recaptcha_hook.js` (manifest content script, MAIN world, document_start on flow.google.com/*) captures the genuine execute before the wrapper, used when a page already has reCAPTCHA. The Labs tab stays for the session cookie / roll. Verified live on token 55 (ext 3.6.1, port 8004): image 11 s, then 7 s + 7 s, 4 s Omni video 38 s, all 200. Codex review (`tmp/codex_captcha_trap_review_0923-1612.md`, ship-with-changes) found 4 edge-case bugs in 3.6.1; **3.6.2** (10:50Z, verified live: image 10 s, video 45 s) fixed the two hook ones, **3.6.3** (published right after, round-2 review `tmp/codex_captcha_trap_review2_0923-1620.md`) the lifecycle ones — the deadline now also bounds the tab loads, mint-tab creation is serialized (warm-up vs request race), a tab that left flow.google.com is never closed, and a last-in-window mint tab is reloaded in place instead of being forgotten: the hook could wipe `grecaptcha.enterprise` when reCAPTCHA's loader re-ran `window.grecaptcha = window.grecaptcha || {}` (now idempotent, and the content script matches only `flow.google.com/about*`, so staff's own project pages are untouched); the genuine-vs-public execute choice happened before `ready` (now inside it, and a page-provided instance without a captured genuine function fails loudly instead of minting a trapped token); cold start + retry could exceed the server's 25 s (mint tab is warmed on connect, an overall deadline is threaded through, a retry replaces only the mint tab, not the Labs tab); the mint-tab id persisted across browser restarts and could name a staff member's unrelated tab (now `chrome.storage.session`, a tab that left flow.google.com is forgotten, never steered or closed; a replaced tab is closed first so nothing leaks). Round 3 (`tmp/codex_captcha_trap_review3_0923-1624.md`) → **3.6.4** (published 11:04Z, verified live on 55: image 9 s, video 35 s): absolute deadline for every mint-tab wait, no double load after a memory-saver discard, a tab someone navigated away during its first load is forgotten, never closed, a still-loading tab stays tracked. Codex's remaining caveats are worst-case only: the Labs-tab ensure and mint pacing are still outside the deadline, and a page that defines `grecaptcha` via `defineProperty` would bypass the hook. Restart/discard/retry paths are unit-tested offline only, not live. Adoption 11:07Z: 6 of 40 accounts on 3.6.x, successes/10 min 0 → 37 → 71. Published via `POST /api/ext/upload` (server serves 3.6.1; staff see the banner within the hour, then must download + reload), also extracted into flow-ultra-01 `/opt/ext` (applies on its next container restart). Container restarted once at 10:23Z to clear token 55's in-memory cooldown (no admin endpoint for that — add one). Rollout to watch: `request_logs` hourly 200 count and `tokens.ext_version` (target 3.6.1 on all 40 active). Not fixed today: server-side Playwright mint through the Oxylabs proxies could not load pages (Page.goto timeout) so there is still no staff-free fallback; `AUTH/ST_EXPIRED` 401s (~15/h, tokens 77/85/55/66) predate the outage and are separate. Account **30** stays `auto_error` from 2026-09-22 (its port 8010 answers again; re-enable when convenient).
- 2026-09-22 (owner asked: match Flow's reference-image limits; owner OK'd deploy overnight): Flow's per-model limits come from `GET aisandbox-pa.googleapis.com/v1/flow/models` (bearer AT, no captcha) — full table and how to re-read it in `docs/flow-model-limits.md`. CONFIRMED: images 10, Omni ingredients 7 (we capped 3), Veo Fast/Lite ingredients 3, Veo Lite ingredients key `veo_3_1_r2v_lite` (we had none). **LIVE** 2026-09-22 ~16:10Z (PR #26, `056f7b2`, container healthy): Omni max 7; new `omni_r2v*` models (every image an ingredient, 1-7); new `veo_3_1_r2v_lite_{landscape,portrait}` (`veo-r2v-lite`; "Veo 3.1 - Lite" + 3 images picks it); images >10 → 400; reference uploads 3 at a time (sequential took ~6.6 s each). Live pre-change probes: Google accepted 20/30 refs on Nano Banana Pro and 20 on Lite without error (unknown if >10 are used). Tests `tests/test_reference_image_limits.py` (16), suite 328 pass / 1 pre-existing. Verified live after deploy: 11 image refs → 400 with a clear message; 10 refs → image in 54 s (uploads 25 s vs 66 s one at a time); "Omni 1.1 Flash" + 7 refs → `abra_r2v_4s` video; `omni-r2v` + 1 ref → `abra_r2v_4s` video; `veo-r2v-lite` + 3 refs → `veo_3_1_r2v_lite` video (all real MP4s). After deploy (16:02-16:08Z): account **30**'s redeem proxy (Oxylabs static `disp.oxylabs.io:8010`) went dead (curl 56 `CONNECT tunnel failed, response 503`; direct curl through it times out while :8004 works) → 10 strikes → `auto_error`, is_active=0. Not caused by the change (no network code touched). Re-enable 30 once :8010 answers again (or give it a new port). Pool note: "No token available for image generation" was already 278/55 min before the deploy (busy/quota), unrelated. Known nit: a refused request is stored as status 500 in `request_logs` while the caller gets 400. Codex out of credits until 2026-09-23 04:44 → Claude-only review; run `cxr tmp/reference_limits_plan.md` on the live code after that. Follow-up (not done): every `*_relaxed` Veo key we expose is in Flow's `deprecatedModels`.
- 2026-09-22 (Claude, flow-ultra-01 setup): two staff-facing fixes in the worker extension **3.5.2** + backend, branch `fix/labs-signin-state`. (1) "Signed in to Google" ≠ "signed in to Google Labs": the popup now says which one is missing and has an "Open Google Labs" button; the session cookie appearing (`cookies.onChanged`) pushes immediately, and `onStartup` pushes too (was install + hourly only) — `worker-extension/session_state.js`, node tests. (2) A NEW account could not register since Flow moved to flow.google.com: Labs' `project.createProject` answers 404 → `Failed to add token`; no account had been added since 8 Sep. The worker now reports the project open on flow.google.com (`project_id`/`project_name` in the push), `add_token` uses it, the pooled extra projects never abort the registration, and the error text tells staff to create a project and Reconnect. Tests: `tests/test_add_token_new_flow.py`. Owner's new Ultra account `nishatmini16@gmail.com` lives in `flow-ultra-01` (Chromium container on cf-worker-01, one static IP disp.oxylabs.io:8005, extension copy with PAC=all hosts; noVNC on the box at 127.0.0.1:6081 via `flow-ultra-novnc` transient unit). Lesson: never `docker rm -f` a live Chromium profile — a hard kill lost the fresh Google cookies once; use `docker stop -t 30`. **Outcome 2026-09-22 14:10Z: the new account does NOT work yet.** It registered as token **93** but INACTIVE (`auto_at_stale`): the access token minted from its live Labs session was refused by `aisandbox-pa …/credits` (401 UNAUTHENTICATED) while token 77's worked in the same test, and minutes later Google signed the account out everywhere (Labs `signin?error=Callback`, flow.google.com back to /about, accounts.google.com "Signed out") — the second time in an hour, this time after a graceful restart with valid cookies on disk. Reading: Google's risk engine revoked the grant and session of a brand-new account (first login on a phone in India, then a shared static proxy IP, bare Chromium). Not fixable in code tonight; the container and the `flow-ultra-novnc` unit stay up for a later sign-in. Owner's established Ultra (id 54, on his Mac) is the working alternative to reserve for pinterest. **14:19Z update:** the owner signed in again (and toggled the extension off/on — Chromium had it disabled until then); token 93 went ACTIVE and served pinterest calls at once — but every generation 500'd with `Failed to prepare project pool: HTTP Error 404` (the same dead Labs createProject, now at generation time) → 10 strikes → `auto_error` ban. Fix: `ensure_project_exists` keeps going with the projects the token already has (branch `fix/project-pool-tolerant`, `tests/test_project_pool_tolerant.py`); token 93 is `reserved_client=pinterest-factory`, re-enabled after PR #25 (`e93d853`) went live 14:25Z, and **serving**: test image with `X-Flow-Client: pinterest-factory` → token 93 success, redeem via its own IP :8005, no watermark (evidence in pinterest-factory `docs/ops-log/assets/2026-09-22-flow-ultra-nishat-*.jpg`); the live pinterest worker lands on it too. `[PROJECT_POOL]` warning fires once per generation on this token (expected: the pool cannot grow past 1).
- 2026-09-22 (found 15 min after PR #22 went live): Pro account **59** refuses every prompt (`PUBLIC_ERROR_UNSAFE_GENERATION` on 16 harmless gift flat-lay prompts, 0 successes today) — a Google-side safety flag on the account. Random rotation had hidden it (1 call all day); Pro-first + least-busy kept picking it because its instant failures made it look idle → 12 caller-facing 500s in 10 min. Mitigation: account 59 disabled via admin API at ~13:56 UTC (owner: re-enable only after Google clears it; a test prompt on it will tell). Fix on branch `fix/flagged-account-cooldown`: `TokenManager._note_prompt_rejection` counts consecutive prompt refusals per account (any success resets); 6 in a row → health cooldown kind `safety_flagged` for 1 h (persisted, shown in the admin reason text), cleared by the next success. Prompt refusals still do not count as strikes. Tests: 4 new (`tests/test_flagged_account_cooldown.py`), suite 308 pass / 1 pre-existing failure.

- 2026-09-22: Owner asked to save Ultra accounts for last. Grilling answers in `tmp/tier_order_plan.md`: images try **Pro → Free → Ultra**, videos **Ultra → Pro → Free**; an idle Ultra is still used when every cheaper account is full/cooling/out of quota; pinterest-factory stays Ultra-only (client routing unchanged); default ON. Built on branch `feat/tier-order`: `call_logic_config.tier_order` (`save_ultra` | `balanced`, guarded ALTER, default save_ultra), `config.tier_order`, stable tier sort in `LoadBalancer.select_token` after the ready/refresh split and before reserved-first (works in both rotation modes), admin `/api/call-logic/config` GET/POST (`tier_order`; saving only `call_mode` keeps it), Settings → Token Rotation Settings → "Account order" dropdown. Tests: 11 new (`tests/test_tier_order.py`), suite 304 pass / 1 pre-existing failure; migration verified on a copy of the live DB. Codex review folded into the 04:51 run (`tmp/codex_watermark_review_prompt.md`).

- 2026-09-22: Owner asked for watermark-free images from every account, with an admin switch to turn it off, and to deploy now and run Codex on the live code once its credits return (2026-09-23 ~04:44). Owner ToS decision: recorded here, owner accepts it. Branch `feat/watermark-removal` (worktree `../flow2api-watermark`). Plan `tmp/watermark_removal_plan.md`; test evidence `tmp/watermark_test/README.md`. What it does: Python port of GargantuaX/gemini-watermark-remover (MIT) calibrated on our own output (48 px logo at 0.60 strength, 73 px margin on 1K, 89 px on 2K; free + Pro are stamped, Ultra is not). Sealed test on real images: 39/41 removed, 0/27 clean images changed. Free/Pro 1K images are downloaded (direct from `flow-content.google`, WARP fallback), cleaned and served from `/tmp` (2 h, same as 2K pins); Ultra keeps Google's link; 2K upscales are cleaned for any tier. Switch: Settings → Generation Settings → "Remove Gemini watermark" (`generation_config.remove_watermark`, default ON, guarded ALTER). C2PA/IPTC "Made with Google AI" metadata and SynthID are deliberately kept (only the visible sparkle goes). Adds `numpy`. Server cost (1 core): 1K ~15 ms, 2K ~75 ms, direct fetch ~45 ms. Tests: 27 new, suite 293 pass / 1 pre-existing failure; migration verified on a copy of the live DB in a throwaway prod-image container (copy deleted). Per-request evidence lands in `request_logs.response_body.generated_assets.watermark` (`applied`, `reason`, `ms`).

- 2026-09-22 (found while testing PR #19 live): prompt rejections and captcha-mint failures wrote `tokens.last_error_at`, a column that exists only on `token_stats` → `sqlite3.OperationalError` → HTTP 500 to the caller (5 of the pinterest worker's last 25 Flow calls; introduced 2026-09-03 in `5206822`). Fix: `db.touch_token_last_error()` stamps `token_stats.last_error_at` without counting an error; both call sites in `token_manager.record_error` use it. Branch `fix/last-error-at-stamp`.

- 2026-09-22: Owner asked for per-caller account routing (content-factory and pinterest-factory share one key; pin images must be watermark-free = Ultra accounts only — owner screenshot: a Pro account's "Visible watermarking" switch is locked ON in this region). **LIVE**: PR #19 merged `4293921` 2026-09-22 ~12:44Z, Coolify auto-deploy healthy ~105 s later; verified with two live calls (`X-Flow-Client: pinterest-factory` → Ultra token 77; no header → default policy) read back from `request_logs.request_body.client`. Pool at deploy: 22 Free / 13 Pro / 7 Ultra active; no account reserved yet. Upstream origin/main (6 commits, plugin cookie sync) deliberately not merged. Caller id = `X-Flow-Client` header (falls back to `X-Client`, which pinterest already sends); no header = `default` policy = unchanged. New `client_policies` table (client → image_tier/video_tier ∈ any|paid|ultra|off, seeded `default=any/any`, `pinterest-factory=ultra/ultra`), new `tokens.reserved_client` (an account only that client may use, tried first, set via `POST /api/tokens/{id}/reserved-client`), one shared filter `client_policy.client_block_reason` used by `select_token`, `get_unavailable_detail` and the admin token diagnostics; the no-account 503 keeps `message/type/status_code` and sets `code: client_policy_no_account` + `client/need/media` (also in the SSE error chunk). Admin page: Settings → "Client Routing" card + "Reserved for app" in the token editor. Tests: 14 new (`tests/test_client_routing.py`), suite 265 pass / 1 pre-existing failure; migration smoke on a copy of the live DB OK. Plan `tmp/client_routing_plan.md` rev 2; Codex out of credits until 2026-09-23 04:44, so the plan review was Claude-only (`tmp/claude_review_client_routing.md`). Next: owner OK → merge to fork `main` (Coolify auto-deploy, brief extension websocket drop) → pinterest side (its branch `feat/flow-client-policy`) → the owner's Ultra account moves from the Mac to a 24×7 Chrome container on cf-worker-01 with the worker extension, watermarking OFF in that account, reserved for `pinterest-factory`.

- 2026-09-17: Owner asked for Suno inside the worker extension with an opt-in switch that exports the person's Suno cookie. Built step 1 on branch `feat/suno-extension-cookie` (plan `tmp/suno_extension_plan.md`, Codex round 1 ship-with-changes, all 9 points folded in; round 2 code review returned ship-with-changes with 3 blocking + 2 minor findings, all fixed: OFF now invalidates before the request exists, identical concurrent uploads serialize per cookie fingerprint, identity must be a string/int, `unchanged` requires a Clerk exchange under 24 h and re-reads billing after 6 h, imports are service-owned tasks drained on close). Backend: `POST /api/plugin/suno-cookie` (plugin connection token, same as the Flow cookie push; shared API key and admin session both rejected) -> `SunoService.sync_account_from_extension`, upsert by Suno user id, one Clerk exchange per upload, `source_client_hash` column (guarded ALTER on start) so a repeat of the same browser cookie on a `ready` account is `unchanged` without Clerk; unhealthy accounts always re-validate; identity required; admin fields untouched. Extension: new `worker-extension/suno.js` (importScripts), switch + status + Sync button in the popup, `cookies.onChanged` debounced re-read, 6 h resync bound, OFF aborts in-flight and invalidates stale work, never routed through `settingsChanged`. Manifest **3.4.1 -> 3.5.0**; zip built at `tmp/Flow2API-Worker-3.5.0.zip`, NOT uploaded (admin `POST /api/ext/upload`) and NOT distributed: gate 9 in `docs/suno-provider.md` (browser/backend session coexistence) must pass first, which needs a real Suno account. Tests: Python 251 pass / 1 pre-existing failure; Node 4 pass. Codex correction recorded: the extension's PAC proxies ONLY Flow + reCAPTCHA hosts; suno.com browsing goes DIRECT from the laptop.

- 2026-09-16: Owner asked to add Suno for music/songs, pointing at `gcui-art/suno-api`. That repo is stale (last push 2026-03-06, 108 open issues) and its generation path is broken upstream: `POST /api/generate/v2/` returns 422 `token_validation_failed` for everyone since Suno's create-page redesign (issues #263/#269, unmerged PR #286), and all of its download paths are dead (#289). So Suno is implemented **natively in Python**, not by wrapping that service. The current contract was re-derived from Suno's own live web bundle on 2026-09-16: generation is `POST /api/generate/v2-web/`, polling is `POST /api/feed/v3`, audio is `GET /api/download/clip/{id}?format=mp3`, and `POST /api/c/check` gates each generation behind hCaptcha (version 1, sitekey d65453de-...) or Turnstile (version 2). Branch `feat/suno-provider`, PR pending. New files: `src/core/suno_models.py`, `src/services/suno_client.py`, `src/services/suno_service.py`, `src/api/suno.py`; tables `suno_accounts`/`suno_jobs` in the existing `flow.db`; no worker-extension change. Codex (high effort) reviewed the plan before implementation and returned ship-with-changes; all seven P1 findings are folded in (captcha-blocked jobs release their slot, ambiguous submits hold it and are never retried, A/B clips settle together, admin auth for credentials, single-flight refresh covering the durable save, atomic dispatch, audio/account lifetime). Reviewed 2026-09-17 before merge: one fix added (a `__client` cookie rotated by an ordinary upstream response, or by the client's own in-place re-mint, was kept only in memory; the service now re-mints 10 s before the client's TTL and persists any rotated jar on next use and at shutdown). Tests: 119 Suno tests pass; full suite 225 pass / 1 pre-existing `test_api_captcha_fingerprint` failure. The fork's `Build and Push Docker Image` GitHub workflow has failed on every push since 2026-09-07 (fork has no registry secrets); production deploys via Coolify, not that workflow. **Not verified against a live Suno account** - owner has not signed up yet; the gate list is section 6 of `docs/suno-provider.md`. The main viability risk is captcha frequency: v1 ships with no captcha provider, so a challenged job is blocked (nothing submitted, nothing charged) rather than solved. 2Captcha was rejected as a recurring cost. Owner ToS decision for Suno automation is NOT yet recorded here and should be before production use.

- 2026-09-15: Owner asked to remove Creaa completely. Branch `remove-creaa` (from `dd6730f`) deletes the Creaa backend (`src/api/creaa.py`, `src/core/creaa_models.py`, `src/services/creaa_bridge.py`), the Creaa extension modules, all Creaa tests/fixtures and `docs/creaa-*.md`; `src/main.py`, `background.js`, `options.*`, extension README and manifest restored to their pre-Creaa (`main` 3272911) state. Extension manifest bumped **3.3.10 -> 3.4.1** so the owner's local 3.4.0 test build updates; globally published package still **3.3.10**. The build method is preserved in `docs/adding-a-browser-provider.md` (SOP for the next site); the Creaa code itself is recoverable at `dd6730f`. PR #16 merged as `c367c2b`; Coolify auto-deploy verified running image `c367c2b` healthy at 2026-09-15 ~01:45 UTC, `/health` 200, `/v1/creaa/models` 404. Stray `creaa.db` deleted from the data volume by hand (69 KB). Branch `codex/creaa-browser-provider` deleted locally and on fork. Next site: Flow Music. Plan locked at rev 3 in `docs/flowmusic-provider-plan.md` after two Codex rounds (both ship-with-changes, all P1s folded in). Design = backend token-row provider (Supabase refresh token held by the backend, API `wb.flowmusic.app`, no anti-bot header seen in the public bundle), not a browser bridge. **Blocked on the owner-run spike (plan section 3)**: needs a signed-in browser on a dedicated Google account; the Claude Chrome extension was not connected. No Flow Music code exists yet. Owner ToS decision for Flow Music automation not yet recorded here.

- Production runs `c367c2b` (PR #16); globally published extension package remains **3.3.10** (repo manifest is 3.4.1, not yet published via the ext-update zip). `feat/async-submit-status` remains unmerged; leave it alone.
- Owner authorized the permanent repository fix and normal Coolify deployment for database thread exhaustion. Branch: `codex/fix-database-thread-leak`. PR #11 merged as `04befb0`; first rollout retained the prior container because Coolify overrides the Docker health command with curl/wget, absent from python:slim. PR #12 added curl to the standard Dockerfile. Coolify production is configured for `/Dockerfile.headed`; follow-up adds curl and the image health check to that file as well. No container patch. PR #13 rollout verified finished on 2026-09-07 11:53 UTC.
- Fix: defer request cancellation until SQLite acquisition/cleanup finishes; shield AnyIO scope cancellation and repeated asyncio task cancellation; allow at most 32 simultaneous connections per Database instance, holding permits until worker threads exit. Writer serialization stays intact.
- Health: `/health` returns HTTP 503 on DB failure; Dockerfile includes a health check. Coolify application health check is enabled for `/health`.
- Validation: 10 new regression tests pass with asyncio and uvloop. Original connection code fails the new cancellation-during-open regression. Full suite: 106 pass, 1 pre-existing failure in `test_api_captcha_fingerprint` (unchanged FlowClient code). Tests cover open/query/close cancellation, repeated cancellation, AnyIO disconnect scopes, 100 competing requests, 200 repeated disconnects, failed opening/configuration, capacity reuse and HTTP health status.
- Upstream `origin/main` contains a README-only chart fix since the production fork; merged before deployment to satisfy the ancestor requirement.

- Linux image verification: 200 isolated disconnects against deployed fix code returned thread count to baseline (1 -> 1).

## Worktrees

- `../flow2api-flagged` — branch `fix/flagged-account-cooldown`, **active** until merged (then safe-to-delete). `../flow2api-watermark` and `../flow2api-tier-order` were removed 2026-09-22 after PRs #21/#22 merged.

## September 7 incident

- At ~11:33 UTC: Python had 37,550 threads and reached container process/thread limit 37,556. Database-backed login and generation raised `RuntimeError: can't start new thread`; HTTP health misleadingly returned 200. Last recorded successful image completed 04:50:54 UTC. All 63 accounts remained saved.
- Owner-authorized recovery used **Coolify CLI**, `coolify app restart jc4044co4w8w0g8w0g4ks0ws`, at ~11:35 UTC. Replacement container `...-113516935692` used the unchanged image. Public authenticated tokens/stats endpoints recovered, browser connections returned, and successful generation resumed by 11:36:16 UTC. Threads fell to 6 and memory to ~211 MiB.
- Isolated reproduction against deployed `aiosqlite==0.20.0`: 10 cancelled connection setups leaked 10 worker threads. Its connection-opening exception handler misses `asyncio.CancelledError`; failed context entry never invokes context exit. This is a proven leak mechanism consistent with exhaustion, though stacks for every pre-restart thread were not captured.
- Evidence on Mac: `/tmp/flow2api-incident-20260907/`. The database and extension settings were not modified during recovery. No runtime-container patch was used.

## Coordination

- Work happens in the owner's Mac clone or `~/projects/flow2api` on the command-center box; `~/mac-mirror/` is read-only.
- Merging to fork `main` auto-deploys via Coolify and briefly drops extension websockets; they reconnect automatically. Notify the owner before rollout to coordinate other sessions.
- Before commit/push/deploy: reread/update this file and include it in the change; prune stale assertions. Never deploy a tree missing current `origin/main` or production `fork/main`.
- Permanent fixes must be committed to the repository and deployed normally. Do not edit running containers or use temporary Docker patches.
